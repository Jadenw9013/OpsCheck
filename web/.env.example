# OpsCheck optional AI briefing. Copy to .env.local and fill in real values.
# .env.local is git-ignored and is never read aloud, printed, or committed.
# The core app, build, and offline tests all work with no key present.

OPSCHECK_AI_ENABLED=false
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5-20251001
